# DealerApp

## Setup

1. Have JadeOrbSchema loaded in your system
2. Load DealerSchema
3. In the application method DealerSchema::initApp set the Root constant to the location of the html templates
4. Run Gameapp

By default it is available on localhost:9999.

You can use separate browsers to simulate multiple players.